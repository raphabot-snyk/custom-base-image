# Custom Base Image Recommendations Setup

## Requirements

* Docker CLI, installed and configured
* SNYK CLI, installed and configured
* curl
* Snyk API Token
* Snyk Org id

## How to Setup
```bash
cd node && ./setup.sh
```
